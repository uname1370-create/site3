import type {NextApiRequest,NextApiResponse} from "next";import formidable,{File} from "formidable";import fs from "fs/promises";export const config={api:{bodyParser:false}};export default async function handler(req:NextApiRequest,res:NextApiResponse){if(req.method!=="POST")return res.status(405).end();try{const data:any=await new Promise((resolve,reject)=>formidable({maxFileSize:5242880}).parse(req,(e,f,files)=>e?reject(e):resolve({fields:f,files})));const x=data.files.image as File|File[];const img=Array.isArray(x)?x[0]:x;if(!img)return res.status(400).json({error:"فقط عکس JPG و PNG قابل قبول است"});const prompt="Apply "+String(data.fields.style)+" with "+String(data.fields.color)+" color on this face. Keep the entire face, skin, hair, lighting and background identical. Only modify the requested beauty area. Natural healed professional permanent makeup result.";const b64=(await fs.readFile(img.filepath)).toString("base64");const uri="data:"+String(img.mimetype||"image/jpeg")+";base64,"+b64;const providers=[["https://api.runware.ai/v1","RUNWARE_API_KEY","flux-kontext"],["https://api.siliconflow.cn/v1","SILICONFLOW_API_KEY","Qwen/Qwen-Image-Edit"],["https://api.aimlapi.com/v1","AIMLAPI_API_KEY","flux/kontext-pro/image-to-image"],["https://gen.pollinations.ai/v1","POLLINATIONS_API_KEY","kontext"]] as const;for(const [base,key,model] of providers){const api=(process.env as any)[key];if(!api)continue;try{const body={model,prompt,image:uri,response_format:"b64_json"};const rr=await fetch(base+"/images/generations",{method:"POST",headers:{"Content-Type":"application/json",Authorization:"Bearer "+api},body:JSON.stringify(body)});if(!rr.ok)continue;const response=await rr.json();const item=response.data?.[0];const url=item?.url||(item?.b64_json?"data:image/png;base64,"+item.b64_json:null);if(url)return res.json({url})}catch{}}return res.status(500).json({error:"خطا در پردازش تصویر. لطفاً دوباره امتحان کنید"})}catch{return res.status(500).json({error:"خطا در پردازش تصویر. لطفاً دوباره امتحان کنید"})}}