import type {NextApiRequest,NextApiResponse} from "next";import fs from "fs/promises";import path from "path";export default async function handler(req:NextApiRequest,res:NextApiResponse){if(req.method!=="POST")return res.status(405).json({error:"روش درخواست مجاز نیست"});try{const p=path.join(process.cwd(),"data/bookings.json");const a=JSON.parse(await fs.readFile(p,"utf8"));a.push({...req.body,timestamp:new Date().toISOString()});await fs.writeFile(p,JSON.stringify(a,null,2));res.json({ok:true})}catch{res.status(500).json({error:"خطا در ثبت نوبت"})}}