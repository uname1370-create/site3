import type {NextApiRequest,NextApiResponse} from "next";import formidable,{File} from "formidable";import fs from "fs/promises";import path from "path";export const config={api:{bodyParser:false}};export default async function handler(req:NextApiRequest,res:NextApiResponse){if(req.headers["x-admin-password"]!==process.env.ADMIN_PASSWORD)return res.status(401).json({error:"رمز عبور نادرست است"});try{const data:any=await new Promise((resolve,reject)=>formidable({maxFileSize:5242880}).parse(req,(e,f,files)=>e?reject(e):resolve({files})));const x=data.files.image as File|File[];const img=Array.isArray(x)?x[0]:x;if(!img||!["image/jpeg","image/png"].includes(img.mimetype||""))return res.status(400).json({error:"فقط عکس JPG و PNG قابل قبول است"});await fs.copyFile(img.filepath,path.join(process.cwd(),"public/hero.jpg"));res.json({ok:true})}catch{res.status(500).json({error:"خطا در بارگذاری تصویر"})}}