# ASAL RAJABI PMU

## REALISTIC PHOTO VIRTUAL PMU ENGINE — IMPLEMENTATION PROMPT

تو به‌عنوان یک **Senior Frontend + Computer Vision + WebGL Engineer** روی پروژه موجود کار می‌کنی.

هدف این مرحله این نیست که یک سایت جدید بسازی یا ظاهر پروژه را عوض کنی.

هدف این است:

> **همین پروژه موجود را حفظ کن و موتور Virtual PMU آن را از یک افکت ساده Canvas به یک سیستم واقعی‌تر، هندسی و Face-Aware تبدیل کن.**

---

# 1. قانون اصلی

قبل از هر تغییری:

1. کل پروژه فعلی را بررسی کن.
2. `index.html` فعلی را دقیق بخوان.
3. تمام JavaScriptهای مربوط به:

   * Face Detection
   * Face Landmarks
   * Eyebrow
   * Lips
   * Eyeliner
   * Canvas
   * Upload
   * Before/After
     را پیدا کن.
4. هیچ بخش سالم UI را حذف نکن.
5. ساختار فعلی پروژه را تا جای ممکن حفظ کن.
6. ابتدا مشخص کن کدام قسمت‌ها قابل استفاده‌اند و فقط همان قسمت‌هایی را که برای Rendering لازم است ارتقا بده.

**ظاهر فعلی، متن‌ها، برند، خدمات، CTAها و Workflow موجود را بی‌دلیل تغییر نده.**

---

# 2. چیزی که نباید بسازی

ممنوع:

* PNG ثابت روی صورت
* تصویر آماده‌ای که فقط جابه‌جا شود
* خط ساده روی ابرو
* یک Polygon ساده به‌عنوان ابرو
* `fillPoly` ساده به‌عنوان نتیجه نهایی
* `addWeighted` ساده بدون تحلیل پوست
* رنگ یکدست روی لب
* eyeliner ثابت که فقط Rotate شود
* Fake AI
* نتیجه‌ای که فقط در یک زاویه کار کند
* تبلیغ «AI» بدون پردازش واقعی

کاربر باید احساس کند:

> «این مدل PMU واقعاً روی صورت خودم شبیه‌سازی شده است.»

نه اینکه یک استیکر روی صورت قرار گرفته باشد.

---

# 3. معماری سبک موردنظر

از معماری بسیار سنگین `v2.md` استفاده نکن.

یک موتور سبک و قابل نگهداری بساز:

```text
Uploaded Photo
      ↓
Image Quality Check
      ↓
MediaPipe Face Landmarker
      ↓
Face Geometry
      ↓
Feature Detection
      ↓
PMU Style Template
      ↓
Mesh / Piecewise Affine Warp
      ↓
Precise Mask
      ↓
Texture Preservation
      ↓
Pigment / Stroke Rendering
      ↓
Lighting Adaptation
      ↓
Natural Blend
      ↓
Final Preview
```

---

# 4. ابزارهای اصلی

در صورت امکان و سازگاری با پروژه فعلی:

### Face Analysis

* MediaPipe Face Landmarker
* Dense facial landmarks
* استفاده از 478 landmark در صورت در دسترس بودن

### Image Processing

* OpenCV.js

برای:

* mask
* blur
* morphology
* color manipulation
* edge-aware processing
* compositing

### Rendering

* WebGL2
* GLSL shaders

در صورت نیاز:

* Three.js

اما Three.js را فقط زمانی اضافه کن که واقعاً لازم باشد.

---

# 5. مهم‌ترین بخش: MICROBLADING

Microblading باید اولویت شماره یک باشد.

## اشتباه فعلی

اگر کد فعلی چیزی شبیه:

```javascript
drawLine(...)
stroke(...)
quadraticCurveTo(...)
```

انجام می‌دهد، آن را به‌عنوان موتور نهایی Microblading قبول نکن.

---

# 6. PMU Brow Template

برای هر مدل ابرو یک Template واقعی تعریف کن.

Template باید شامل اطلاعاتی مانند:

```javascript
{
    name,
    browShape,
    controlPoints,
    archPosition,
    headPosition,
    apexPosition,
    tailPosition,
    thickness,
    hairStrokes: [...]
}
```

هر Hair Stroke باید اطلاعات هندسی داشته باشد:

```javascript
{
    start,
    end,
    curve,
    angle,
    length,
    width,
    opacity,
    darkness
}
```

حداقل چند ده stroke برای هر ابرو ایجاد شود.

ترجیحاً:

```text
40–100+ realistic strokes
```

نه یک خط ضخیم.

---

# 7. Face-Adaptive Brow Geometry

ابتدا نقاط مهم ابرو و صورت را از Face Landmarker استخراج کن.

حداقل:

```text
brow head
brow body
brow arch
brow tail
upper brow boundary
lower brow boundary
eye reference
nose reference
forehead reference
```

سپس Template را با ابعاد صورت کاربر تطبیق بده.

نباید فقط:

```text
translate
rotate
scale
```

انجام شود.

از یکی از این روش‌ها استفاده کن:

### ترجیح اول

Piecewise Affine Mesh Warp

یا:

### گزینه دوم

Thin Plate Spline

یا:

### گزینه سوم

Barycentric / Triangle Mesh Warp

---

# 8. Brow Mesh

برای ابرو یک Mesh ایجاد کن.

مثلاً:

```text
P1 ---- P2 ---- P3 ---- P4
 |      |      |      |
P5 ---- P6 ---- P7 ---- P8
 |      |      |      |
P9 ---- P10 --- P11 --- P12
```

Template روی این Mesh ساخته شود.

سپس Mesh با Face Geometry کاربر deformation شود.

هدف:

> شکل و انحنای ابروی انتخاب‌شده حفظ شود ولی با فرم واقعی صورت کاربر منطبق گردد.

---

# 9. Hair Stroke Rendering

هر Hair Stroke باید:

* جهت طبیعی داشته باشد
* ضخامت متغیر داشته باشد
* opacity متغیر داشته باشد
* کمی curvature داشته باشد
* با زاویه مناسب نسبت به موقعیت ابرو تغییر کند
* در Head/BODY/TAIL رفتار متفاوت داشته باشد

مثلاً:

### Head

strokeهای کوتاه‌تر و نسبتاً عمودی

### Body

strokeهای مورب و follow-the-form

### Tail

strokeهای باریک‌تر و کشیده‌تر

---

# 10. Skin Texture Preservation

این قسمت بسیار مهم است.

بعد از رندر ابرو:

**پوست نباید پلاستیکی شود.**

Texture اصلی پوست باید باقی بماند.

از روش‌هایی مثل:

* luminance preservation
* alpha compositing
* soft mask
* local blending
* edge-aware blending

استفاده کن.

فرمول ساده:

```text
Final =
Original Skin Texture
+
PMU Pigment
+
Hair Stroke Detail
```

نه:

```text
Final = Solid Brow Color
```

---

# 11. Lighting Adaptation

رنگ PMU نباید در تمام تصویر یک مقدار ثابت داشته باشد.

نور تقریبی پوست اطراف ناحیه را تحلیل کن.

سپس شدت pigment را متناسب با:

```text
local luminance
local contrast
skin brightness
```

تنظیم کن.

---

# 12. Brow Mask

برای ابرو:

```text
Outer Brow Mask
+
Inner Feather
-
Unwanted Area
```

ایجاد کن.

Mask باید:

* نرم باشد
* لبه مصنوعی نداشته باشد
* با پوست blend شود
* در Tail نرم‌تر شود.

از Gaussian Blur یا روش مناسب دیگر برای feather استفاده کن.

---

# 13. LIPS

برای Lip Shading نیز همین منطق را استفاده کن.

ابتدا:

```text
Outer Lip Boundary
Inner Lip Boundary
Mouth Opening
Teeth Region
```

را استخراج کن.

رنگ فقط روی بخش صحیح لب اعمال شود.

---

# 14. Lip Rendering

از:

```text
Original Luminance
+
Pigment Color
+
Soft Mask
```

استفاده کن.

رنگ را روی کل لب به‌صورت Flat Paint نکن.

قسمت‌های روشن و تیره طبیعی لب حفظ شوند.

لب باید:

* حجم داشته باشد
* texture داشته باشد
* highlight حفظ شود
* contour طبیعی باقی بماند.

---

# 15. EYELINER

Eyeliner باید براساس شکل واقعی پلک کاربر ساخته شود.

از landmarkهای اطراف چشم استفاده کن.

ابتدا curve ایجاد کن:

```text
Inner Corner
      ↓
Lash Line
      ↓
Outer Corner
      ↓
Wing
```

Curve باید Face-Aware باشد.

ضخامت eyeliner در طول curve ثابت نباشد.

مثلاً:

```text
Inner → thin
Middle → medium
Outer → stronger
Wing → controlled taper
```

---

# 16. PMU STYLE SYSTEM

کد را طوری بساز که بعداً بتوان مدل‌های بیشتری اضافه کرد.

مثلاً:

```javascript
PMU_STYLES = {
    microblading_natural: {...},
    microblading_soft: {...},
    microblading_defined: {...},
    lip_shading_natural: {...},
    eyeliner_classic: {...}
}
```

فعلاً لازم نیست تعداد زیادی مدل بسازی.

**اول یک مدل Microblading را واقعاً خوب پیاده کن.**

بعد معماری را برای مدل‌های دیگر آماده نگه دار.

---

# 17. Before / After

سیستم Before/After فعلی را حفظ کن.

اما خروجی جدید باید همان تصویر کاربر باشد که واقعاً پردازش شده است.

نباید:

* تصویر Demo
* عکس آماده
* نمونه ساختگی

به‌جای نتیجه کاربر نمایش داده شود.

---

# 18. Image Quality Check

قبل از پردازش:

بررسی کن:

* صورت پیدا شده؟
* صورت بیش از حد کوچک است؟
* نور کافی است؟
* صورت خیلی چرخیده است؟
* کیفیت عکس قابل قبول است؟
* چشم‌ها/ابروها قابل مشاهده‌اند؟

اگر مناسب نبود:

```text
لطفاً یک عکس واضح‌تر و روبه‌رو ارسال کنید.
```

نمایش بده.

---

# 19. Multiple Face Handling

اگر عکس چند نفر داشت:

به‌صورت پیش‌فرض:

```text
Largest / Most Central Face
```

را انتخاب کن.

یا از کاربر بخواه فقط یک چهره در تصویر باشد.

---

# 20. Performance

پردازش را طوری انجام بده که موبایل ضعیف نیز بتواند Preview بگیرد.

قانون:

```text
Preview Resolution
≠
Original Resolution
```

ابتدا Preview را روی resolution مناسب پردازش کن.

بعد در صورت نیاز خروجی نهایی را با کیفیت بالاتر تولید کن.

از پردازش غیرضروری جلوگیری کن.

---

# 21. Web Worker

اگر پردازش باعث Block شدن UI می‌شود:

Face Analysis / OpenCV processing را تا حد امکان به:

```text
Web Worker
```

منتقل کن.

اما اگر پیچیدگی بیش از فایده شد، برای نسخه اول ضروری نیست.

---

# 22. Rendering Architecture

Rendering را از UI جدا کن.

مثلاً:

```text
VirtualPMUEngine
│
├── FaceAnalyzer
├── FaceGeometry
├── BrowEngine
├── LipEngine
├── EyelinerEngine
├── PMUTemplates
├── MaskEngine
├── WarpEngine
├── BlendEngine
└── QualityCheck
```

اما اگر پروژه فعلی تک‌فایل است، فقط در صورتی فایل‌های جدید بساز که واقعاً ضروری باشد.

**هدف Modular بودن است، نه زیاد کردن تعداد فایل‌ها.**

---

# 23. استفاده از GitHub Projects به‌عنوان Reference

قبل از پیاده‌سازی، این پروژه‌ها را بررسی کن:

### FaceStudio

```text
georgegach/facestudio
```

برای:

* MediaPipe Face Landmarker
* 478 landmarks
* WebGL2
* Piecewise Affine Warp
* Face-aware rendering
* texture preservation

### OpenMakeupSDK

```text
ehsanwwe/OpenMakeupSDK
```

برای:

* WebGL
* GLSL
* Face Mesh
* makeup geometry
* eyeliner
* facial deformation

### virtual-makeup

```text
otdnnc/virtual-makeup
```

برای:

* MediaPipe
* OpenCV.js
* masks
* browser-side processing

### Jayanths9/Virtual_Makeup

برای:

* 478 landmarks
* eyebrow geometry
* lip masks
* eyeliner

از این پروژه‌ها **کپی کورکورانه نکن**.

فقط معماری و تکنیک‌های قابل استفاده را بررسی کن.

License هر repository را قبل از reuse بررسی کن.

---

# 24. معیار مهم برای نتیجه

نتیجه باید روی این موارد تست شود:

### Test A

صورت زن با ابروی کم‌پشت

### Test B

صورت با ابروی پرپشت

### Test C

صورت با پوست روشن

### Test D

صورت با پوست تیره‌تر

### Test E

صورت کمی چرخیده

### Test F

صورت با نور ضعیف

### Test G

صورت با نور جانبی

### Test H

عکس موبایلی معمولی

در تمام این موارد:

* Template نباید از صورت جدا شود.
* ابرو نباید شناور باشد.
* لبه‌ها نباید مصنوعی باشند.
* پوست نباید محو شود.
* هویت شخص نباید تغییر کند.

---

# 25. Debug Mode

یک Debug Mode داخلی ایجاد کن که بتواند در توسعه نشان دهد:

```text
Face Landmarks
Brow Landmarks
Lip Landmarks
Eye Landmarks
Brow Mask
Lip Mask
Eyeliner Curve
Warp Mesh
PMU Strokes
```

این حالت برای کاربر نهایی نمایش داده نشود.

---

# 26. مهم‌ترین تست

قبل از اضافه کردن امکانات جدید:

فقط این را کامل کن:

```text
Upload Photo
      ↓
478 Face Landmarks
      ↓
Brow Geometry
      ↓
One Microblading Template
      ↓
Mesh Warp
      ↓
Realistic Hair Strokes
      ↓
Skin Texture Preservation
      ↓
Natural Blend
      ↓
Before / After
```

اگر این مسیر واقعاً خوب شد، سپس Lip و Eyeliner را ارتقا بده.

---

# 27. معیار پذیرش Microblading

Microblading فقط زمانی Done محسوب می‌شود که:

1. روی ابروی واقعی کاربر قرار بگیرد.
2. نسبت به فرم صورت تغییر شکل دهد.
3. Strokeها با فرم ابرو هماهنگ شوند.
4. Strokeها Flat Line نباشند.
5. پوست زیر و اطراف ابرو دیده شود.
6. لبه‌ها طبیعی باشند.
7. رنگ با نور صورت هماهنگ شود.
8. در حالت Before/After واضح ولی طبیعی باشد.
9. با چند نوع صورت تست شده باشد.
10. روی حداقل 5–10 عکس متفاوت قابل قبول باشد.

---

# 28. UI را خراب نکن

این پروژه یک وب‌سایت زیبایی Premium است.

بنابراین:

* UI فعلی حفظ شود.
* رنگ‌بندی برند حفظ شود.
* Mobile-first باقی بماند.
* Upload تجربه ساده داشته باشد.
* نتیجه سریع نمایش داده شود.
* Before/After فعلی حفظ شود.
* Booking فعلی حفظ شود.

هیچ redesign بزرگی انجام نده مگر اینکه برای عملکرد جدید ضروری باشد.

---

# 29. خروجی نهایی Agent

بعد از پیاده‌سازی، گزارش بده:

### Existing Code

چه قسمت‌هایی از کد قبلی حفظ شد؟

### Replaced

کدام قسمت‌ها تغییر کرد؟

### New Engine

چه چیزی برای Rendering واقعی اضافه شد؟

### Face Detection

چه تکنولوژی استفاده شد؟

### Warp

دقیقاً از چه روش deformation استفاده شد؟

### Mask

چطور ساخته شد؟

### Texture

چطور حفظ شد؟

### Microblading

چطور Template و Strokeها ساخته شدند؟

### Lips

چطور پردازش شدند؟

### Eyeliner

چطور روی پلک منطبق شد؟

### Performance

روی موبایل چگونه مدیریت شد؟

### Testing

روی چند عکس تست شد؟

### Remaining Limitations

چه محدودیت‌هایی هنوز وجود دارد؟

---

# 30. قانون نهایی

**هیچ نتیجه‌ای را Fake یا شبیه‌سازی کلامی اعلام نکن.**

اگر یک قابلیت واقعاً پیاده نشده است، صریح بنویس:

```text
NOT IMPLEMENTED
```

اگر یک تکنیک فقط Prototype است، بنویس:

```text
PROTOTYPE
```

اگر واقعاً Face-Aware و Warp شده است:

```text
IMPLEMENTED
```

---

# FINAL PRIORITY

ترتیب اولویت دقیقاً این باشد:

```text
1. REAL FACE LANDMARKS
2. FACE GEOMETRY
3. PMU TEMPLATE
4. MESH WARP
5. PRECISE MASK
6. REALISTIC STROKES / PIGMENT
7. SKIN TEXTURE PRESERVATION
8. LIGHTING ADAPTATION
9. NATURAL BLENDING
10. BEFORE / AFTER
11. PERFORMANCE
12. UI POLISH
```

**هدف این پروژه ساخت یک فیلتر آرایشی نیست.**

هدف:

> ساخت یک Photo-Based Virtual PMU Preview است که کاربر بتواند عکس واقعی خودش را وارد کند و مدل انتخابی Microblading / Lip Shading / Eyeliner را با هندسه متناسب با چهره خودش، به‌صورت طبیعی روی همان عکس مشاهده کند.

اول **Microblading را به سطح قابل‌قبول واقعی برسان**؛ سپس همان موتور را برای Lip Shading و Eyeliner توسعه بده.

در تمام مراحل، **کد موجود را قبل از تغییر درک کن، از بازنویسی غیرضروری جلوگیری کن و نتیجه واقعی را بر حجم کد ترجیح بده.**
