# راهنمای اصلی موتور Virtual PMU — هدف کاربر، منطق خروجی و الزامات پیاده‌سازی

## 1. هدف اصلی کاربر

این پروژه یک ابزار Virtual PMU برای وب‌سایت عسل رجبی PMU است.

کاربر سایت باید بتواند یک عکس واقعی از چهره خود را انتخاب کند و سپس سرویس‌هایی مثل «میکروبلیدینگ» را روی همان چهره امتحان کند.

اصل پروژه این نیست که یک خط، PNG، استیکر یا طرح ثابت روی صورت قرار داده شود.

هدف این است که سیستم، فرم واقعی ابروی همان شخص را تحلیل کند و نتیجه‌ای تولید کند که از نظر بصری شبیه این باشد که ابروی واقعی او بعد از انجام میکروبلیدینگ تغییر کرده است.

---

## 2. مهم‌ترین تفاوت با پیاده‌سازی فعلی

پیاده‌سازی نامطلوب:

`میکروبلیدینگ → رسم یک خط یا شکل آماده روی صورت`

این روش قابل قبول نیست.

پیاده‌سازی هدف:

`عکس کاربر → تشخیص چهره → تشخیص دقیق ابرو → استخراج هندسه ابرو → ساخت تارهای میکروبلیدینگ متناسب با همان ابرو → تغییر فرم و تراکم ابرو → ماسک دقیق → ترکیب با پوست و نور واقعی → خروجی طبیعی`

یعنی نتیجه باید **خود ابروی کاربر را تغییر دهد**، نه اینکه صرفاً یک علامت گرافیکی روی صورت نمایش دهد.

---

## 3. رفتار مورد انتظار در گزینه «میکروبلیدینگ»

وقتی کاربر روی:

**میکروبلیدینگ**

کلیک می‌کند:

1. عکس انتخاب‌شده کاربر باید همان عکس اصلی باقی بماند.
2. سیستم باید چهره را تشخیص دهد.
3. نقاط دقیق اطراف هر دو ابرو باید استخراج شوند.
4. فرم فعلی هر ابرو باید محاسبه شود.
5. فرم هدف میکروبلیدینگ باید نسبت به هندسه همان ابرو ساخته شود.
6. تارهای جدید باید به‌صورت مجزا و متعدد ایجاد شوند.
7. هر تار باید جهت، طول، ضخامت، opacity و موقعیت متناسب با ناحیه ابرو داشته باشد.
8. تارها باید روی هندسه واقعی صورت Warp شوند.
9. نتیجه باید داخل محدوده واقعی ابرو و ناحیه هدف قرار گیرد.
10. بافت پوست زیر ابرو نباید نابود شود.
11. نور و سایه عکس اصلی باید حفظ شود.
12. رنگ و شدت پیگمنت باید با ابروی واقعی و نور عکس سازگار شود.
13. مرز ماسک نباید به شکل یک خط یا لکه مصنوعی دیده شود.
14. خروجی نهایی باید به‌صورت یک ابروی واقعی‌تر، مرتب‌تر و پرتر دیده شود.

---

## 4. مفهوم «پرپشت شدن» که باید در کد پیاده شود

اگر ابروی ورودی کم‌پشت باشد، خروجی نباید فقط یک خط بالای ابرو یا داخل ابرو ایجاد کند.

باید:

- نواحی خالی بین تارهای موجود تشخیص داده شوند.
- فرم کلی ابرو اصلاح شود.
- در صورت نیاز تاج، بدنه، قوس و دم ابرو بازسازی شوند.
- تارهای جدید بین تارهای موجود قرار بگیرند.
- تراکم در قسمت‌های مختلف یکنواخت نباشد.
- تاج ابرو طبیعی‌تر و پراکنده‌تر باشد.
- بدنه تراکم بیشتری داشته باشد.
- دم ابرو جهت‌دار و ظریف‌تر شود.
- طول و زاویه تارها بر اساس موقعیتشان تغییر کند.
- تارهای جدید با تارهای طبیعی موجود ادغام شوند.

هدف، تولید یک «بافت ابرو» است؛ نه تولید یک «خط ابرو».

---

## 5. هندسه باید Face-Specific باشد

هیچ Template ثابتی نباید بدون تغییر روی تمام چهره‌ها قرار گیرد.

Template می‌تواند فقط به‌عنوان تعریف سبک استفاده شود.

برای هر عکس:

- Face landmarks
- Brow landmarks
- Brow contour
- Brow width
- Brow height
- Brow arch
- Brow head
- Brow body
- Brow tail
- Face scale
- Face rotation
- Local brow orientation

باید در محاسبات استفاده شوند.

فرم نهایی باید به هندسه همان چهره وابسته باشد.

---

## 6. Template میکروبلیدینگ باید واقعی باشد

یک Template معتبر میکروبلیدینگ نباید فقط شامل یک polygon یا خط باشد.

Template باید بتواند موارد زیر را تعریف کند:

- مسیر پایه ابرو
- نقاط کنترل
- قوس
- ضخامت هدف
- محدوده تاج
- محدوده بدنه
- محدوده دم
- مجموعه تارهای مو
- زاویه هر تار
- طول هر تار
- ضخامت هر تار
- opacity هر تار
- شدت pigment
- تغییر تراکم در طول ابرو
- variation طبیعی
- جهت رشد مو

در حالت ایده‌آل، حداقل ده‌ها تار و در نسخه نهایی تعداد بیشتری stroke تولید شود.

---

## 7. Warp باید روی هر تار یا mesh انجام شود

برای طبیعی شدن نتیجه، نباید یک تصویر کامل ابرو را resize و روی صورت قرار داد.

روش مطلوب:

**Stroke/mesh → Face/Brow geometry → Non-rigid warp → Mask → Blend**

روش‌های قابل بررسی:

- Piecewise Affine
- Barycentric deformation
- Thin Plate Spline
- Mesh Warp
- Curve deformation

انتخاب روش باید بر اساس کد فعلی پروژه و performance مرورگر انجام شود.

---

## 8. ماسک

ماسک باید فقط ناحیه هدف ابرو را پوشش دهد.

باید از:

- eyebrow contour
- surrounding landmarks
- feathering
- local opacity

استفاده شود.

لبه ماسک نباید قابل تشخیص باشد.

نباید یک مستطیل، polygon سخت یا خط رنگی اطراف ابرو دیده شود.

---

## 9. حفظ پوست

یکی از مهم‌ترین الزامات:

**بافت پوست باید باقی بماند.**

رندر نباید:

- پوست را صاف و پلاستیکی کند
- ناحیه ابرو را یک‌دست رنگ کند
- texture را حذف کند
- جزئیات طبیعی پوست را از بین ببرد

ترجیحاً از روش‌هایی مانند:

- luminance preservation
- alpha blending
- texture-aware compositing
- multiply/overlay مناسب
- local color adaptation

استفاده شود.

---

## 10. حفظ هویت چهره

سیستم نباید:

- فرم کلی صورت را تغییر دهد
- چشم‌ها را تغییر دهد
- بینی یا لب را تغییر دهد
- صورت را بازسازی کند
- چهره را generative بازتولید کند

در نسخه فعلی، هدف **ویرایش موضعی و deterministic روی عکس اصلی** است.

---

## 11. عکس نمونه و معیار ارزیابی

در پروژه دو عکس نمونه مخصوص تست وجود دارد:

- `m2.png`
- `m3.png`

این دو فایل باید به‌عنوان نمونه‌های فعلی حفظ شوند.

**آن‌ها را با فایل‌های `img2/2.JPG`, `img2/3.JPG` یا `img2/4.JPG` جایگزین نکنید و به دکمه‌های نمونه متصل نکنید، مگر اینکه کاربر صراحتاً درخواست کند.**

هدف تست نمونه‌ها این است که کاربر بتواند عکس را انتخاب کند و سپس تغییر واقعی PMU را روی همان عکس ببیند.

---

## 12. معیار قبولی خروجی

خروجی زمانی قابل قبول است که:

- ابروی واقعی فرد قابل تشخیص بماند.
- تغییر، فقط یک خط روی صورت نباشد.
- ضخامت و تراکم ابرو واقعاً تغییر کرده باشد.
- تارهای جدید در ساختار ابرو دیده شوند.
- فرم ابرو متناسب با چهره باشد.
- تاج، قوس، بدنه و دم رفتار متفاوت و طبیعی داشته باشند.
- تغییرات به عکس اصلی متصل باشند.
- پوست و نور اصلی حفظ شوند.
- نتیجه در اندازه واقعی عکس نیز طبیعی باشد.
- با تغییر عکس ورودی، هندسه خروجی نیز تغییر کند.

---

## 13. تستی که Agent باید انجام دهد

Agent نباید فقط کد را بخواند و بگوید «پیاده شده».

باید ابتدا مسیر واقعی اجرای پروژه را پیدا کند:

1. صفحه اصلی
2. دکمه نمونه
3. انتخاب عکس
4. `processImage`
5. face detection
6. landmarks
7. crop
8. service selection
9. `paintMakeup`
10. microblading renderer
11. mask
12. blend
13. canvas output

سپس مشخص کند دقیقاً کدام تابع چه کاری انجام می‌دهد.

---

## 14. بررسی کد فعلی

Agent باید ابتدا کد موجود را بررسی کند و فقط بر اساس کد واقعی گزارش دهد.

مخصوصاً باید مشخص کند:

- آیا Face Landmarker واقعاً اجرا می‌شود؟
- چند landmark استفاده می‌شود؟
- آیا brow landmarks به‌صورت semantic استفاده می‌شوند؟
- آیا renderer فعلی stroke واقعی دارد؟
- آیا strokeها مستقل هستند؟
- آیا template به چهره warp می‌شود؟
- آیا ماسک دقیق است؟
- آیا skin texture حفظ می‌شود؟
- آیا خروجی فقط line/curve است؟
- آیا opacity و blending طبیعی است؟
- آیا service selection واقعاً renderer را تغییر می‌دهد؟
- آیا sample selection واقعاً `m2.png` و `m3.png` را وارد pipeline می‌کند؟

---

## 15. قانون بسیار مهم برای تغییر کد

بدون بررسی ساختار فعلی پروژه، فایل جدید بزرگ ایجاد نکن.

اول:

**CODE AUDIT**

بعد:

**MINIMAL SAFE CHANGE**

بعد:

**TEST**

و سپس:

**REPORT**

نباید برای حل این مسئله ساختار اصلی پروژه بی‌دلیل بازنویسی شود.

---

## 16. چیزی که نباید ساخته شود

این موارد به‌عنوان راه‌حل نهایی قابل قبول نیستند:

- یک خط ساده روی ابرو
- یک SVG ثابت روی صورت
- PNG ثابت روی صورت
- یک مستطیل یا polygon رنگی
- یک mask ساده بدون stroke structure
- resize کردن یک عکس ابرو
- copy/paste یک ابروی آماده
- تغییر کل چهره با AI
- خروجی generative که هویت فرد را تغییر دهد
- شبیه‌سازی صرفاً نمایشی بدون تغییر واقعی ساختار ابرو

---

## 17. هدف نهایی

کاربر باید وقتی روی «میکروبلیدینگ» کلیک می‌کند، این احساس را داشته باشد:

«این همان عکس خودم است، اما ابروهای خودم واقعاً با میکروبلیدینگ پرتر و اصلاح‌شده شده‌اند.»

نه اینکه:

«یک خط یا شکل روی ابروی من کشیده شده است.»

این تفاوت، معیار اصلی کل توسعه Virtual PMU در این پروژه است.

---

## 18. ترتیب کار Agent

ترتیب اجرای کار:

### مرحله 1 — Audit
تمام کد مرتبط با Virtual PMU را پیدا و بررسی کن.

### مرحله 2 — Trace
مسیر کامل عکس از ورودی تا خروجی را trace کن.

### مرحله 3 — Gap Analysis
مشخص کن چرا خروجی فعلی به‌جای تغییر واقعی ابرو، فقط خط/طرح ایجاد می‌کند.

### مرحله 4 — Design
بر اساس ساختار فعلی پروژه، معماری کم‌ریسک برای stroke-based brow rendering طراحی کن.

### مرحله 5 — Implementation
فقط قسمت‌های لازم را اصلاح کن.

### مرحله 6 — Validation
با `m2.png` و `m3.png` تست کن.

### مرحله 7 — Report
گزارش بده:

- چه چیزی پیدا شد
- مشکل اصلی کجا بود
- چه فایل‌هایی تغییر کردند
- چه توابعی تغییر کردند
- چه چیزی هنوز باقی مانده
- آیا خروجی واقعاً face-specific شده است یا نه
- تست با هر دو sample چه نتیجه‌ای داده است

---

## 19. اولویت

اولویت‌ها:

1. Face-specific eyebrow transformation
2. Realistic microblading strokes
3. Natural brow geometry
4. Accurate mask
5. Skin texture preservation
6. Natural blending
7. Identity preservation
8. Performance
9. UI polish

ظاهر UI نباید جایگزین کیفیت واقعی PMU شود.

---

## 20. اصل نهایی

**Virtual PMU در این پروژه باید یک image-compositing/geometry problem واقعی باشد، نه یک overlay UI problem.**

هر تغییری که فقط ظاهر دکمه یا نمایش یک خط را بهتر کند ولی ساختار واقعی ابرو را تغییر ندهد، مسئله اصلی را حل نکرده است.
