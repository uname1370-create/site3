# گزارش نهایی — عسل رجبی PMU (site3)

تاریخ: ۲۰۲۶-۰۹-۲۰ · شاخه: `arena/01a0bcfc-site3`

وب‌سایت پریمیوم + **موتور تست مجازی واقعی** میکروبلیدینگ، شیدینگ لب و خط چشم.
هر نتیجه از هندسهٔ واقعی صورتِ همان عکس محاسبه می‌شود؛ هیچ اورلی ثابت، کوردینت
سخت‌کدشده یا فیلتر جعلی وجود ندارد.

---

## ۱. ممیزی (Audit) وضعیت موجود

پروژهٔ موجود قبل از شروع:

| فایل | وضعیت | تصمیم |
|---|---|---|
| `index.html` | فرودسایت + اسکریپت **جعلی** درون‌خطی | بازنویسی کامل (سیستم طراحی حفظ شد) |
| `admin.html` | پنل مدیریت آتلانتیس (۳۲۸ خط) | دست‌نخورده — جزو پروژهٔ اصلی، بی‌ربط به موتور تست |
| `asalrajabi.png`, `m2.png`, `m3.png` | عکس‌های برند/نمونه | حفظ شدند (همان فایل‌ها) |
| بقیه | نبود | — |

**نقض‌های شناسایی‌شده در اسکریپت جعلی قدیمی:**

1. **کوردینت‌های ثابت** (`LEFT_BROW`, `LIP_OUTER`, …) — ابرو/لب/خط‌چشم روی
   هر عکسی در همان نقطهٔ صفحه کشیده می‌شد (قیفِ استیکر، نه تست روی صورت).
2. **بدون هیچ تشخیص چهره** — نه لندمارک، نه gate کیفیت؛ نتیجه روی هر تصویری
   «موفق» نمایش داده می‌شد.
3. **ادعای جعلی رزرو** — «نوبتت ثبت شد» بدون هیچ ارسال واقعی.
4. **شماره واتساپ اشتباه** (۹۸۹۱۵۰۰۰۰۰۰۰۰) در کد.
5. **`ctx.filter`** — در Safari iOS غیرفعال است؛ لبه‌های محو روی آیفون شکست می‌خورد.
6. PWA نبود: نه manifest، نه icon، نه service worker.

**تصمیم معماری (بر اساس اولویت‌های پرامپت):**

- استک فعلی (صفحهٔ استاتیک + Tailwind CDN + Vazirmatn) **حفظ** شد؛ پرامپت
  React/Next/TS/Framer را «فقط در صورت نیاز» می‌دانست و این موتور با ماژول‌های
  ES خالص کار می‌کند.
- **OpenCV.js خارج شد**: MediaPipe Tasks Vision (`face-landmarker`) ۴۷۸
  لندمارک + ماژول‌های تصویرساز خالص (blur/erode/dilate/کامپوننت‌ها) همهٔ
  کارهای «OpenCV-like» را با یک وابستگی سبک‌تر می‌کنند.
- **Canvas2D** برای رندر (به‌جای WebGL)؛ `ctx.filter` هرگز استفاده نشد
  (سازگار با Safari). رنگ‌ها در فضای **LAB** پردازش می‌شوند.
- همه‌چیز **رایه‌ای**؛ پردازش عکس کاملاً **خارج از مرورگر کاربر** (کلاینت‌ساید).

---

## ۲. تغییرات (Changes)

| مسیر | نوع | توضیح |
|---|---|---|
| `index.html` | بازنویسی | head PWA (manifest/icon/theme-color)، بخش تست مجازی با سلايدر قبل/بعد، چیپ‌های خدمات (میکروبلیدینگ/شیدینگ لب/خط چشم/**ریموو**)، اسلایدر شید، دکمه‌های رندر HQ/دانلود، یادداشت حریم خصوصی (فقط بعد از رندر واقعی)، کارت تماس، سیت رزرو، پنل دیباگ `?debug=1` |
| `sw.js` | جدید | service worker: precache پوسته، navigation network-first، هم‌خاسته cache-first، CDN stale-while-revalidate |
| `manifest.webmanifest` | جدید | `fa`/`rtl`، standalone، theme `#0F3D2E` |
| `icons/*.png` | جدید | ۱۹۲/۵۱۲/maskable/apple-۱۸۰ — با `tools/make-icons.mjs` تولید شدند |
| `js/tryon/**` | جدید (۱۵ ماژول) | کل موتور تست مجازی (بخش ۳) |
| `test/engine.test.mjs` | جدید | ۳۳ تست Node خالص |
| `tools/make-icons.mjs` | جدید | انکودر PNG بدون وابستگی (zlib+CRC32) + لوگوی برند رویارو |
| `README.md` | بازنویسی | مستندسازی پروژه |
| `admin.html`, عکس‌ها, `.gitignore` | دست‌نخورده | — |

---

## ۳. ماژول‌های جدید (New Modules)

```
js/tryon/
├── app.js            811  UI orchestrator: upload → gate → render → slider → booking
├── booking.js         53  منطق خالص رزرو: normalizePhone, buildWaDraft, متن‌های دقیق
├── faceAnalyzer.js   221  MediaPipe FaceLandmarker (GPU→CPU) + gate‌های کیفیت فارسی
├── landmarks.js      219  ۴۷۸ لندمارک → مناطق (faceBounds, ابروها, چشم‌ها, لب)
├── pose.js            76  تخمین yaw/pitch/roll + کیفیت ژست
├── pipeline.js       152  renderTryOn(): کیفیت fast (720px) / high (1600px)
├── styles.js          27  تعریف سبک‌ها (id/name/hint/param)
├── brow/
│   ├── analyze.js    340  تشخیص ابروی واقعی کاربر از پیکسل (لومینانس + threshold شکاف)
│   ├── model.js      329  PMUBrowModel + fitBrow (TPS/affine قطعه‌ای: مقیاس+چرخش+خم+غیرصلب)
│   └── render.js     169  کشیدن hair-strokeها روی سطح warp‌شده با Pigment در LAB
├── lip/
│   ├── colorize.js    58  رنگ‌آمیزی LAB با حفظ L (روشنایی پوست)
│   └── render.js     157  ماسک نرم لب؛ دهان داخلی و دندان هرگز رنگ نمی‌خورند
├── liner/
│   ├── analyze.js    120  منحنی مژه از لندمارک → مسیر + پهنای متغیر + بال
│   └── render.js     121  Pigment لبه-نرم روی مسیر
├── debug/overlay.js  148  لایه‌های دیباگ (landmarks/ROI/natural/target/control points/strokes)
└── lib/
    ├── vec.js        460  ریاضی ۲بعدی: Catmull-Rom, Delaunay, hull, affine قطعه‌ای
    ├── image.js      229  boxBlur, Otsu, erode/dilate, کامپوننت‌های بزرگ‌ترین, polygonMask
    ├── lab.js         78  RGB↔LAB (گامای صحیح)
    └── canvasPath.js  70  کمکی‌های مسیر Canvas
```

معماری: **منطق تجاری خارج از UI** — `booking.js` خالص و تست‌پذیر است؛
`app.js` فقط چسب‌اندازی DOM؛ `pipeline.js` فقط ورودی‌های خالص + بازگشت canvas.

---

## ۴. منابع مرجع (References)

| مرجع | چه چیزی | کجا/چرا استفاده شد | لایسنس |
|---|---|---|---|
| [ehsanwwe/OpenMakeupSDK](https://github.com/ehsanwwe/OpenMakeupSDK) | معماری میکاپ مبتنی بر MediaPipe + WebGL؛ رندر region-based؛ API ماژولار npm-style | الگو برای **معماری رندر region-based** و تفکیک ماژول‌ها | **MIT** — فقط ایده؛ کد کپی نشده (در صورت استفادهٔ مستقیم، ارجاع MIT لازم است) |
| [georgegach/facestudio](https://github.com/georgegach/facestudio) | piecewise-affine warp؛ feather ماسک؛ similarity fit اجزای قاعده‌ای؛ واحدتست ریاضی خالص | الگو برای **ریاضی warp** در `lib/vec.js`، feathering ماسک و **استراتژی تست** (ریاضی خالص جدا از DOM) | **GPL-3.0** — **ناسازگار** با محصول؛ فقط مفهوم، هیچ کدی استفاده نشد |
| [otdnnc/virtual-makeup](https://github.com/otdnnc/virtual-makeup) | Face Landmarker در مرورگر + ماسک region به region | تأیید رویکرد **FaceLandmarker Tasks داخل مرورگر** + ماسک منطقه‌ای | بدون لایسنس (حقوق محفوظ) — فقط مفهوم |
| [Jayanths9/Virtual_Makeup](https://github.com/Jayanths9/Virtual_Makeup) | ترکیب میکاپ در فضای LAB «تا رنگ روی هر تن پوست یکسان بنشیند و بافت لب حفظ شود» | مستقیم تأییدکنندهٔ **رویکرد رنگ‌آمیزی LAB** در `lip/colorize.js` و pigmentation ابرو/لاینر | بدون لایسنس — فقط مفهوم |

> هیچ قطعه کدی از هیچ مرجع کپی نشده است؛ همهٔ کد این ریپازیتوری اصلی است.

---

## ۵. شرح موتور (Engine Description)

جریان کامل برای هر عکس:

1. **دریافت عکس** — فایل/کشیدن-ریختن/عکس نمونه → `Image` در حافظه (هیچ آپلودی به سرور).
2. **تشخیص** (`faceAnalyzer.js`) — MediaPipe FaceLandmarker ۴۷۸ لندمارک
   (GPU→CPU fallback). درصورت هر شکست: پیام فارسی مشخص + دکمهٔ «دوباره تلاش کن»
   (چهره پیدا نشد / چند چهره / رزولوشن کم: عرض صورت <۳۰۰px یا ارتفاع <۳۴۰px /
   ژست شدید / تاریک <۴۲ / سوختم >۲۴۲ / نور تخت std<۹ / ابرو یا لب خارج از فریم).
3. **مناطق** (`landmarks.js` + `pose.js`) — faceBounds، نوک بینی، لبه‌های
   ابرو/چشم هر طرف، لب (بیرونی/بالا/پایین/درون دهان/دندان).
4. **تست ابروی واقعی کاربر** (`brow/analyze.js`) — لومینانس ستون‌به‌ستون،
   میانهٔ هر ستون، تشخیص باندهای تاری (ابروروز) با **آستانهٔ مبتنی بر شکاف**
   (نه Otsu که برای نوار باریک شکست می‌خورد)، run-های متصل، تمیزکاری مورفولوژیک،
   gate‌های پوشش/پهنای/تاریکی. خروجی: چندضلعی و نقاط ابروی **واقعی** کاربر.
5. **fit مدل PMU** (`brow/model.js`) — `PMUBrowModel` (controlPoints + hair-strokeها
   با position/angle/length/width/opacity/curvature + thickness/arch/tailAngle/
   colorProfile). `fitBrow` با **TPS/affine قطعه‌ای** مدل قاعده‌ای را به هندسهٔ
   ابروی کاربر warp می‌کند: مقیاس + چرخش + انحنای خاص خودش + تبدیل غیرصلب —
   هرگز scale یکنواخت نیست.
6. **رندر** (`brow/render.js`, `lip/`, `liner/`) —
   - *میکروبلیدینگ*: هر stroke روی سطح warp‌شده با گرادیان + لبهٔ نرم؛ pigmentation
     در **LAB** با حفظ L پوست (بافت/نور پوست زیر خط‌ها باقی می‌ماند؛ ابروی سیاهِ
     یک‌تکه ساخته نمی‌شود).
   - *شیدینگ لب*: رنگ‌آمیزی LAB فقط در نواحی بیرونی/بالا/پایین لب؛
     **دهان داخلی و دندان خارج از ماسک**؛ feather + edge falloff؛ بدون fill تخت.
   - *خط چشم*: منحنی از **لندمارک‌های واقعی مژه** → curve fit → مسیر → پهنای
     متغیر → بال با lift → pigmentation لبه‌نرم. هیچ خط ثابت نیست.
7. **دو حالت** — `fast` (720px، strokeScale ۰٫۶۵، برای پیش‌نمایش لحظه‌ای موبایل) و
   `high` (1600px، برای خروجی/دانلود).
8. **خروجی UI** — slider قبل/بعد (pointer + touch)، اسلایدر شید (debounce ۲۰۰ms
   و رندر مجدد)، دکمهٔ «✦ رندر با کیفیت بالا»، دانلود PNG (همان نمای crop شده)،
   CTA «✨ همین مدل رو می‌خوام».

**حریم خصوصی:** یادداشت «پردازش عکس در مرورگر شما انجام می‌شود» **فقط** بعد از
یک رندر واقعی نشان داده می‌شود (درست است: هیچ دیتایی ارسال نمی‌شود؛ حتی مدل
MediaPipe از CDN لود و در کش مرورگر می‌ماند).

---

## ۶. تست (Testing)

`node test/engine.test.mjs` → **۳۳/۳۳ سبز**:

- `vec.js` — resample یکنواخت؛ pointInPoly؛ convexHull؛ Catmull-Rom؛
  **Delaunay**: تعداد دقیق مثلث‌ها (۳۰ نقطه → ۴۷ = 2n−2−h)، استفادهٔ همهٔ نقاط،
  **صفر نقض دایرهٔ محیطی**، کارکرد در مقیاس پیکسلی (۳۰۰۰px).
- `image.js` — boxBlur، Otsu، erode/dilate، بزرگ‌ترین کامپوننت، polygonMask.
- `lab.js` — round-trip RGB↔LAB (گاما).
- `pose.js` — تخمین yaw/pitch/roll روی ژست‌های مصنوعی.
- `landmarks.js` — صحت مناطق ساخته‌شده.
- `brow/analyze.js` — **تشخیص ابروی واقعی**: نوار تاری مصنوعی شناخته می‌شود،
  عکس بدون ابرو رد می‌شود (gate).
- `brow/model.js` — دقت fit (نقاط تطبیق روی هندسهٔ هدف)، mirror چپ/راست.
- `lip/colorize.js` — **L حفظ می‌شود** (روشنایی پوست)، a/b به سمت رنگ سبک می‌رود،
  ناحیهٔ داخلی لب دست‌نخورده.
- `liner/analyze.js` — مسیر روی خط مژه، بال به بیرون+بالا، mirror دقیق دو طرف.
- `booking.js` — **متن موفقیت دقیقاً** «درخواست شما آماده ارسال برای هماهنگی است.»؛
  disclaimer هرگز رزرو را تأیید نمی‌کند؛ normalizePhone (۰۹/۹۸+۹۸/۰۰۹۸ + رد
  شماره‌های نامعتبر)؛ فرمت دقیق پیش‌نویس واتساپ.

درون مرورگر: `?debug=1` → پنل لایه‌های دیباگ (لندمارک‌های ۴۷۸تایی، ROI،
ابروی طبیعی vs هدف، نقاط کنترل warp، strokeها) — مخصوص توسعه، پنهان از کاربر.

**آزمون دستی پیشنهادی طبق پرامپت** (روی مرورگر واقعی): تصاویر oval/round/long/
heart، فاصلهٔ چشم‌ها، شکل‌های ابرو؛ موبایل/روشنایی روز/درونی/نزدیک/متوسط؛
پوست روشن/متوسط/تیره؛ هر ۳ سرویس. (برای این بخش، مرورگر واقعی نیاز است —
در محیط توسعه بدون مرورگر انجام نشد و صادقانه گزارش می‌شود.)

---

## ۷. محدودیت‌های شناخته‌شده (Known Limitations)

1. **ابروروز خیلی کم‌رنگ** (تقریباً هم‌رنگ پوست) یا سایهٔ شدید — تشخیص‌گر
   لومینانس ممکن است ابرو را کامل تشخیص ندهد؛ در این حالت gate رد می‌کند و پیام
   فارسی می‌دهد (به‌جای نتیجهٔ بد).
2. **جز و مژگان سنگین/لب‌های خیلی باز** — هندسهٔ لب/لاینر از لندمارک می‌آید؛
   لبخندهای شدید ممکن است دقت لب را کم کند (گیت ژست جلوی بدترین موارد را می‌گیرد).
3. **تک‌چهره** — چهره‌های دوم و بعد (سلفی با یکی دیگر) رد می‌شوند.
4. **مدل ابروی قاعده‌ای تکی** — طبق ترتیب توسعهٔ پرامپت «اول یک مدل بسیار
   باکیفیت»؛ ۳ سبک (میکرو/پودر/طبیعی) روی همان مدل با تفاوت در چگالی/پهنای/
   شفافیت stroke تعریف شده‌اند، نه assetهای جدا.
5. **CDN برای MediaPipe** (wasm+model ≈ چند MB) — بار اول نیاز به اینترنت؛
   بعداً توسط service worker کش می‌شود (offline بعد از اولین بازدید).
6. **Canvas2D** — در مقیاس فعلی کافی است؛ برای ماسک‌های خیلی بزرگ‌تر/سرعت
   بیشتر، WebGL به‌عنوان بهینه‌سازی بعدی (اثری بر خروجی ندارد).
7. تست‌های داخل مرورگر (e2e) و آزمون دستی ماتریسی صورت/پوست در این محیط
   (بدون مرورگر) انجام **نشده** — فقط تست‌های ریاضی/منطق خالص ۳۳تایی سبزند.

---

## ۸. بهبودهای بعدی (Next Improvements)

1. **WebGL composite pass** برای ترکیب نهایی (سرعت + ماسک‌های بزرگ).
2. **assetهای مجزای ابرو** برای هر سبک (پس از تأیید مدل تکی روی ۵–۱۰ صورت واقعی).
3. **Worker** برای analyze (خارج از main thread؛ UI کاملاً fluid بماند).
4. **e2e با Playwright** (Chrome headless) روی مجموعهٔ تست ۵–۱۰ عکس با مقایسهٔ
   متریک‌های خروجی + آزمون ماتریسی صورت/پوست/نور پرامپت.
5. **استهبال محلی** (self-host) مدل/wasm MediaPipe برای حذف وابستگی CDN.
6. **رفاین generative اختیاری** (حفظ هویت) — طبق پرامپت فقط به‌عنوان لایهٔ
   اختیاری بعدی، نه هستهٔ سیستم.
7. **پیش‌نمایش زنده/کامerascope** — خارج از v1 (طبق تصمیم «فقط عکس»).
8. **A/B برچسب‌گذاری سبک‌ها** بر اساس بازخورد واقعی کاربران از سیت رزرو.

---

## اجرا

```bash
# تست موتور (بدون مرورگر)
node test/engine.test.mjs

# سایت
python3 -m http.server 8080 --bind 0.0.0.0
# → http://localhost:8080  (PWA: installable در Chrome Android و Safari iOS)
# → http://localhost:8080/?debug=1  (پنل دیباگ توسعه)

# آیکون‌ها (در صورت نیاز)
node tools/make-icons.mjs
```
